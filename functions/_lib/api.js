import { runPersonalScan, runCompanyScan, runSocialScan, runAccountsScan, reportToMarkdown, reportToCsv, scoreRisk, hashIdentifier, createFinding, accountTaskFromPlatform, maskIdentifier, scanGithubPublic } from '../../src/scanners/core/runtime.mjs';
import { runExternalChecks } from '../../src/scanners/core/external-adapters.mjs';
import { runHibpEmailRange } from '../../src/scanners/core/hibp-adapter.mjs';

const cache = new Map();
const DEFAULT_PLATFORM_CATALOG = [
  { id: 'github', name: 'GitHub', domain: 'github.com', identifiers: ['email', 'username'], recoveryEntry: 'https://github.com/password_reset', dataExportEntry: 'https://github.com/settings/export', deletionEntry: 'https://github.com/settings/admin', cleanupNote: 'Review security settings, SSH keys, tokens and public repositories.' },
  { id: 'google', name: 'Google', domain: 'google.com', identifiers: ['email', 'phone', 'username'], recoveryEntry: 'https://accounts.google.com/signin/recovery', dataExportEntry: 'https://takeout.google.com/', deletionEntry: 'https://myaccount.google.com/delete-services-or-account', cleanupNote: 'Review account security, connected apps and Takeout exports.' },
  { id: 'microsoft', name: 'Microsoft', domain: 'microsoft.com', identifiers: ['email', 'phone', 'username', 'gamertag'], recoveryEntry: 'https://account.live.com/password/reset', dataExportEntry: 'https://account.microsoft.com/privacy', deletionEntry: 'https://account.live.com/closeaccount.aspx', cleanupNote: 'Review recovery methods and connected devices.' },
  { id: 'facebook', name: 'Facebook', domain: 'facebook.com', identifiers: ['email', 'phone', 'username'], recoveryEntry: 'https://www.facebook.com/login/identify', dataExportEntry: 'https://www.facebook.com/dyi', deletionEntry: 'https://www.facebook.com/help/delete_account', cleanupNote: 'Review public profile, sessions and data export.' },
  { id: 'x', name: 'X / Twitter', domain: 'x.com', identifiers: ['email', 'phone', 'username'], recoveryEntry: 'https://x.com/account/begin_password_reset', dataExportEntry: 'https://x.com/settings/download_your_data', deletionEntry: 'https://x.com/settings/deactivate', cleanupNote: 'Review login methods and public profile exposure.' },
  { id: 'linkedin', name: 'LinkedIn', domain: 'linkedin.com', identifiers: ['email', 'phone', 'username'], recoveryEntry: 'https://www.linkedin.com/checkpoint/rp/request-password-reset', dataExportEntry: 'https://www.linkedin.com/psettings/member-data', deletionEntry: 'https://www.linkedin.com/help/linkedin/answer/a1339364', cleanupNote: 'Review public résumé fields and data export.' },
  { id: 'wechat', name: 'WeChat', domain: 'wechat.com', identifiers: ['phone', 'wechat_id', 'nickname'], recoveryEntry: 'https://help.wechat.com/', dataExportEntry: 'https://help.wechat.com/', deletionEntry: 'https://help.wechat.com/', cleanupNote: 'Review phone binding, recovery entries and privacy settings.' }
];
const USERNAME_PUBLIC_PLATFORMS = [
  ['github','GitHub','https://github.com/{u}','global'],['gitlab','GitLab','https://gitlab.com/{u}','global'],['gitee','Gitee','https://gitee.com/{u}','china'],['npm','npm','https://www.npmjs.com/~{u}','global'],['pypi','PyPI','https://pypi.org/user/{u}/','global'],['dockerhub','Docker Hub','https://hub.docker.com/u/{u}','global'],['huggingface','Hugging Face','https://huggingface.co/{u}','global'],['kaggle','Kaggle','https://www.kaggle.com/{u}','global'],['medium','Medium','https://medium.com/@{u}','global'],['devto','DEV','https://dev.to/{u}','global'],['keybase','Keybase','https://keybase.io/{u}','global'],['reddit','Reddit','https://www.reddit.com/user/{u}/','global'],['mastodon','Mastodon.social','https://mastodon.social/@{u}','global'],['producthunt','Product Hunt','https://www.producthunt.com/@{u}','global'],['stackoverflow','Stack Overflow','https://stackoverflow.com/users/{u}','global'],['codepen','CodePen','https://codepen.io/{u}','global'],['behance','Behance','https://www.behance.net/{u}','global'],['dribbble','Dribbble','https://dribbble.com/{u}','global'],['patreon','Patreon','https://www.patreon.com/{u}','global'],['twitch','Twitch','https://www.twitch.tv/{u}','global'],['telegram','Telegram public','https://t.me/{u}','global'],['youtube','YouTube handle','https://www.youtube.com/@{u}','global'],['bilibili','Bilibili search-like handle','https://space.bilibili.com/{u}','china'],['zhihu','Zhihu','https://www.zhihu.com/people/{u}','china'],['csdn','CSDN','https://blog.csdn.net/{u}','china'],['juejin','稀土掘金','https://juejin.cn/user/{u}','china'],['cnblogs','博客园','https://www.cnblogs.com/{u}/','china'],['segmentfault','SegmentFault','https://segmentfault.com/u/{u}','china'],['jianshu','简书','https://www.jianshu.com/u/{u}','china'],['douban','豆瓣','https://www.douban.com/people/{u}/','china'],['weibo','微博','https://weibo.com/{u}','china'],['xiaohongshu','小红书','https://www.xiaohongshu.com/user/profile/{u}','china'],['kuaishou','快手','https://www.kuaishou.com/profile/{u}','china']
].map(([id,name,url,region]) => ({ id, name, url, region }));
const STATUS_ORDER = ['已确认有账号/登录记录', '可能存在账号', '候选待确认', '未发现证据', '跳过', '查询失败', '待人工确认'];
const response = (data, status = 200, type = 'application/json; charset=utf-8') => new Response(type.includes('json') ? JSON.stringify(data, null, 2) : data, { status, headers: { 'content-type': type, 'cache-control': 'no-store' } });
const db = (env = {}) => env.DB || env.OYB_DB || env.OYB_DATABASE || null;
const safeMsg = (error = {}) => String(error?.message || error || 'unknown_error').replace(/(Bearer\s+)[A-Za-z0-9._~-]+/gi, '$1[redacted]').replace(/(key=)[^\s&]+/gi, '$1[redacted]').replace(/(authorization|cookie|token|secret|password)[^,;\n]*/gi, '$1=[redacted]').slice(0, 240);
const cleanId = (id = '') => String(id || '').replace(/^report_/, '');
const now = () => new Date().toISOString();
const localId = (prefix = 'id') => `${prefix}_${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 8)}`;
function jsonError(code, status = 500, message = code, detail) { return response({ error: { code, message, ...(detail ? { detail } : {}) } }, status); }
async function body(request) { try { return await request.json(); } catch { return {}; } }
function findLocal(id = '') { return cache.get(cleanId(id)) || cache.get(id); }
function gate(request, env = {}) { if (request.method !== 'GET' && !(request.headers.get('idempotency-key') || request.headers.get('x-idempotency-key'))) return jsonError('MISSING_IDEMPOTENCY_KEY', 400, 'POST requests require an Idempotency-Key header.'); const token = request.headers.get('x-private-token') || request.headers.get('authorization') || ''; if (request.method !== 'GET' && env.SCAN_MODE !== 'private' && token.length < 12) return jsonError('MISSING_AUTH_TOKEN', 401, 'Private mode token is required for write operations.'); return null; }
function ensureCatalog(payload = {}) { payload.platformCatalog = Array.isArray(payload.platformCatalog) && payload.platformCatalog.length ? payload.platformCatalog : DEFAULT_PLATFORM_CATALOG; return payload; }
function normalizeIdentifierList(p = {}) { return (p.identifiers || []).map(x => String(x).toLowerCase()); }
function accountStatusMeta(task = {}) { if (task.statusLabel) return { statusLabel: task.statusLabel, statusLevel: task.statusLevel || 'info', evidenceSource: task.evidenceSource || 'provided_status', statusReason: task.statusReason || '按报告证据判断。' }; const raw = `${task.accountStatus || ''} ${task.taskType || ''} ${task.notes || ''} ${task.evidenceSource || ''}`.toLowerCase(); if (/adapter_error|query_failed|lookup_failed|failed/.test(raw)) return { statusLabel: '查询失败', statusLevel: 'medium', evidenceSource: 'adapter_error', statusReason: '查询源返回错误、网络失败或平台风控，不能据此判断账号不存在。' }; if (/adapter_status|skipped|missing_|not_configured|authorization|limited/.test(raw)) return { statusLabel: '跳过', statusLevel: 'info', evidenceSource: 'adapter_skipped', statusReason: '缺少授权、密钥、实名或用户数据，当前未执行该项查询。' }; if (/no_match|not_found|zero|未发现/.test(raw)) return { statusLabel: '未发现证据', statusLevel: 'info', evidenceSource: 'no_evidence_found', statusReason: '已检查的当前数据源未发现该平台线索，不代表平台绝对没有账号。' }; if (/confirmed|password_manager|public_profile_confirmed/.test(raw)) return { statusLabel: '已确认有账号/登录记录', statusLevel: 'high', evidenceSource: 'public_profile_or_user_record', statusReason: '公开主页或你提供的记录中出现该平台线索。' }; if (/mailbox|browser|platform_export|possible|public_profile_uncertain/.test(raw)) return { statusLabel: '可能存在账号', statusLevel: 'medium', evidenceSource: 'public_profile_uncertain_or_user_trace', statusReason: '公开探测返回受限/重定向，或用户材料中出现平台线索，需要人工核验。' }; if (/candidate_from_username|username_review|username_nickname_candidate|candidate/.test(raw)) return { statusLabel: '候选待确认', statusLevel: 'low', evidenceSource: 'username_nickname_catalog_candidate', statusReason: '仅根据用户名/昵称和平台目录生成候选任务，不代表已经确认存在账号。' }; return { statusLabel: '待人工确认', statusLevel: 'info', evidenceSource: 'manual_review_required', statusReason: '当前证据不足，需要通过官方入口人工确认。' }; }
function enrichAccountStatuses(report = {}) { report.accountTasks = (report.accountTasks || []).map(task => ({ ...task, ...accountStatusMeta(task) })); const summary = Object.fromEntries(STATUS_ORDER.map(k => [k, 0])); for (const task of report.accountTasks) summary[task.statusLabel] = (summary[task.statusLabel] || 0) + 1; report.accountStatusSummary = summary; report.platformAccountStatus = report.accountTasks.map(t => ({ platformId: t.platformId || '', platformName: t.platformName || '', status: t.statusLabel, statusLevel: t.statusLevel, evidenceSource: t.evidenceSource, reason: t.statusReason, actionUrl: t.actionUrl || t.recoveryEntry || t.dataExportEntry || t.deletionEntry || '' })).slice(0, 300); report.accountStatusLegend = { confirmed: '已确认：公开主页 200/用户上传记录/官方授权结果。', possible: '可能存在：公开探测受限、重定向或用户材料间接线索。', candidate: '候选：目录候选，不等同命中。', noEvidence: '未发现证据：当前源未发现，不代表绝对没有。', skipped: '跳过：缺配置、授权或平台不支持。', failed: '查询失败：网络、风控或接口错误。', unknown: '待人工确认：证据不足。' }; return report; }
async function runDb(statement, binds = []) { return statement.bind(...binds).run(); }
async function saveReport(env = {}, report = {}) { const d = db(env); if (!d?.prepare || !report?.id) return { persisted: false, reason: 'D1 binding DB is not available.' }; const created = report.generatedAt || now(); const userId = 'local'; const subject = JSON.stringify(report.maskedSubject || {}); try { await runDb(d.prepare('insert or replace into reports (id,user_id,job_id,report_type,risk_score,report_json,created_at) values (?,?,?,?,?,?,?)'), [report.id, userId, report.scanId, report.reportType, Number(report.riskScore?.total || 0), JSON.stringify(report), created]); await runDb(d.prepare('insert or replace into jobs (id,user_id,subject_type,subject_hash,mode,status,progress,started_at,finished_at,error_message,created_at) values (?,?,?,?,?,?,?,?,?,?,?)'), [report.scanId, userId, report.reportType, await hashIdentifier(subject, env.HASH_SALT || 'open-your-box'), report.reportType, 'completed', 100, created, created, '', created]); await runDb(d.prepare('delete from findings where job_id = ?'), [report.scanId]); for (const finding of report.findings || []) await runDb(d.prepare('insert or replace into findings (id,job_id,user_id,source,category,severity,confidence,title,summary,evidence_type,evidence_preview,evidence_ref,remediation_json,created_at) values (?,?,?,?,?,?,?,?,?,?,?,?,?,?)'), [finding.id, report.scanId, userId, finding.source, finding.category, finding.severity, finding.confidence, finding.title, finding.summary, finding.evidenceType, finding.evidencePreview, finding.evidenceRef || '', JSON.stringify(finding.remediation || {}), finding.createdAt || created]); for (const task of report.accountTasks || []) await runDb(d.prepare('insert or replace into account_tasks (id,user_id,platform_id,platform_name,account_status,task_type,task_status,action_url,notes,created_at,updated_at) values (?,?,?,?,?,?,?,?,?,?,?)'), [task.id, userId, task.platformId || '', task.platformName || '', task.statusLabel || task.accountStatus || 'possible', task.taskType || 'review', task.taskStatus || 'todo', task.actionUrl || task.recoveryEntry || task.dataExportEntry || task.deletionEntry || '', `${report.scanId} · ${task.statusLabel || ''} · ${task.statusReason || ''} · ${task.notes || ''}`.slice(0, 900), task.createdAt || created, task.updatedAt || created]); return { persisted: true }; } catch (error) { return { persisted: false, error: { code: 'D1_WRITE_FAILED', message: safeMsg(error) } }; } }
async function readReport(env = {}, id = '') { const local = findLocal(id); if (local) return enrichAccountStatuses(local); const d = db(env); if (!d?.prepare) return null; const key = cleanId(id); try { const row = await d.prepare('select report_json from reports where id = ? or job_id = ? or id = ? limit 1').bind(id, key, `report_${key}`).first(); return row?.report_json ? enrichAccountStatuses(JSON.parse(row.report_json)) : null; } catch { return null; } }
async function dnsJson(name = '', type = 'MX') { const res = await fetch(`https://cloudflare-dns.com/dns-query?name=${encodeURIComponent(name)}&type=${encodeURIComponent(type)}`, { headers: { accept: 'application/dns-json' } }); if (!res.ok) throw new Error(`DNS ${type} HTTP ${res.status}`); return res.json(); }
function emailDomain(email = '') { const m = String(email || '').trim().toLowerCase().match(/^[^@\s]+@([a-z0-9.-]+\.[a-z]{2,})$/); return m?.[1] || ''; }
async function checkEmailMx(email = '', scanId = 'scan') { const domain = emailDomain(email); if (!domain) return { finding: createFinding({ scanId, source: 'email_identifier_api', category: 'format_error', severity: 'low', confidence: 'verified', title: '邮箱格式无效', summary: '邮箱格式不符合基本规则，未执行 MX 检查。', evidenceType: 'self_declared_identifier', evidencePreview: 'invalid email format' }), status: { identifierType: 'email', label: '邮箱', provider: 'dns_mx', apiExecuted: false, deterministicResult: true, valid: false, reason: '邮箱格式无效。' } }; try { const data = await dnsJson(domain, 'MX'); const records = Array.isArray(data.Answer) ? data.Answer : []; const valid = records.length > 0; return { finding: createFinding({ scanId, source: 'email_identifier_api', category: 'mx_lookup', severity: valid ? 'info' : 'medium', confidence: 'verified', title: valid ? '邮箱域名 MX 检查通过' : '邮箱域名未发现 MX 记录', summary: valid ? '已通过 DNS 查询确认邮箱域名存在可收信 MX 记录；这只能证明域名具备收信能力，不能证明该邮箱注册过某个平台。' : 'DNS 查询未发现邮箱域名 MX 记录；请确认邮箱是否填写正确。', evidenceType: 'dns_mx', evidencePreview: `${domain} · mx_records=${records.length}`, affectedIdentifierMasked: maskIdentifier(email, 'email') }), status: { identifierType: 'email', label: '邮箱', provider: 'dns_mx', apiExecuted: true, deterministicResult: true, valid, reason: valid ? '已确认邮箱域名具备收信 MX 记录；不是账号注册命中。' : '未发现邮箱域名 MX 记录；不是账号注册命中。' } }; } catch (error) { return { finding: createFinding({ scanId, source: 'email_identifier_api', category: 'mx_lookup_error', severity: 'info', confidence: 'low', title: '邮箱域名 MX 检查失败', summary: safeMsg(error), evidenceType: 'dns_mx', evidencePreview: domain, affectedIdentifierMasked: maskIdentifier(email, 'email') }), status: { identifierType: 'email', label: '邮箱', provider: 'dns_mx', apiExecuted: true, deterministicResult: false, valid: null, reason: `MX 查询失败：${safeMsg(error)}` } }; } }
function classifyChinaPhone(raw = '') { const digits = String(raw || '').replace(/\D/g, '').replace(/^86/, ''); const valid = /^1[3-9]\d{9}$/.test(digits); const prefix3 = digits.slice(0,3), prefix4 = digits.slice(0,4); let carrier = 'unknown'; if (/^(134|135|136|137|138|139|147|148|150|151|152|157|158|159|172|178|182|183|184|187|188|195|197|198)$/.test(prefix3) || /^(1703|1705|1706)$/.test(prefix4)) carrier = 'China Mobile'; else if (/^(130|131|132|145|146|155|156|166|171|175|176|185|186|196)$/.test(prefix3) || /^(1704|1707|1708|1709)$/.test(prefix4)) carrier = 'China Unicom'; else if (/^(133|149|153|173|174|177|180|181|189|190|191|193|199)$/.test(prefix3) || /^(1700|1701|1702)$/.test(prefix4)) carrier = 'China Telecom'; else if (/^(192)$/.test(prefix3)) carrier = 'China Broadnet'; return { valid, normalized: valid ? `+86${digits}` : '', carrier, lineType: valid ? 'mobile_cn' : 'unknown', reason: valid ? `中国大陆手机号格式有效，基础号段运营商推断为 ${carrier}。这不是平台账号命中。` : '未识别为中国大陆 11 位手机号。' }; }
async function lookupPhone(phone = '', env = {}, scanId = 'scan') { const raw = String(phone || '').trim(); if (!raw) return null; const masked = maskIdentifier(raw, 'phone'); const cn = classifyChinaPhone(raw); const configured = env.TWILIO_ACCOUNT_SID && env.TWILIO_AUTH_TOKEN ? 'twilio' : env.NUMVERIFY_API_KEY ? 'numverify' : env.ABSTRACT_PHONE_API_KEY ? 'abstractapi' : ''; if (!configured) return { finding: createFinding({ scanId, source: 'phone_identifier_api', category: 'local_cn_lookup', severity: cn.valid ? 'info' : 'medium', confidence: 'verified', title: cn.valid ? '手机号基础格式/号段检查通过' : '手机号基础格式/号段未确认', summary: `${cn.reason} 未配置外部 Lookup provider，因此没有执行外部手机号有效性/归属地 API，更没有执行平台账号命中查询。`, evidenceType: 'phone_lookup', evidencePreview: `${masked} · ${cn.carrier}`, affectedIdentifierMasked: masked }), status: { identifierType: 'phone', label: '手机号', provider: 'local_cn_prefix', apiExecuted: true, deterministicResult: true, valid: cn.valid, carrier: cn.carrier, lineType: cn.lineType, reason: cn.reason } }; try { let provider = configured, valid = null, detail = ''; if (configured === 'twilio') { const auth = btoa(`${env.TWILIO_ACCOUNT_SID}:${env.TWILIO_AUTH_TOKEN}`); const res = await fetch(`https://lookups.twilio.com/v2/PhoneNumbers/${encodeURIComponent(raw)}`, { headers: { authorization: `Basic ${auth}` } }); const data = await res.json().catch(() => ({})); valid = res.ok && Boolean(data.phone_number); detail = data.country_code || data.national_format || `HTTP ${res.status}`; } else if (configured === 'numverify') { const res = await fetch(`https://apilayer.net/api/validate?access_key=${encodeURIComponent(env.NUMVERIFY_API_KEY)}&number=${encodeURIComponent(raw)}&format=1`); const data = await res.json().catch(() => ({})); valid = data.valid === true; detail = [data.country_name, data.carrier, data.line_type].filter(Boolean).join(' · ') || `valid=${String(data.valid)}`; } else if (configured === 'abstractapi') { const res = await fetch(`https://phonevalidation.abstractapi.com/v1/?api_key=${encodeURIComponent(env.ABSTRACT_PHONE_API_KEY)}&phone=${encodeURIComponent(raw)}`); const data = await res.json().catch(() => ({})); valid = data.valid === true; detail = [data.country?.name, data.carrier, data.type].filter(Boolean).join(' · ') || `valid=${String(data.valid)}`; } return { finding: createFinding({ scanId, source: 'phone_identifier_api', category: 'lookup_summary', severity: valid ? 'info' : 'medium', confidence: 'verified', title: valid ? '手机号 Lookup 已执行：有效' : '手机号 Lookup 已执行：未确认有效', summary: `已调用 ${provider} Lookup。该结果只能说明号码格式/归属/运营商类信息，不能证明该手机号注册过某个平台。`, evidenceType: 'phone_lookup', evidencePreview: `${masked} · ${provider} · ${detail || 'no detail'}`, affectedIdentifierMasked: masked }), status: { identifierType: 'phone', label: '手机号', provider, apiExecuted: true, deterministicResult: true, valid, localCn: cn, reason: `已执行 ${provider} Lookup：${detail || '无摘要'}。这不是平台账号命中。` } }; } catch (error) { return { finding: createFinding({ scanId, source: 'phone_identifier_api', category: 'lookup_error', severity: 'info', confidence: 'low', title: '手机号 Lookup 执行失败', summary: safeMsg(error), evidenceType: 'phone_lookup', evidencePreview: masked, affectedIdentifierMasked: masked }), status: { identifierType: 'phone', label: '手机号', provider: configured, apiExecuted: true, deterministicResult: false, valid: null, localCn: cn, reason: `手机号 Lookup 失败：${safeMsg(error)}` } }; } }
async function identifierApiStatus(payload = {}, env = {}, scanId = 'scan') { const ids = payload.identifiers || {}, findings = [], status = []; if (ids.email) { const mx = await checkEmailMx(ids.email, scanId); findings.push(mx.finding); status.push(mx.status); } if (ids.phone) { const phone = await lookupPhone(ids.phone, env, scanId); if (phone) { findings.push(phone.finding); status.push(phone.status); } } return { findings, status }; }
function identifierCoverageMatrix(payload = {}, scanId = 'scan') { const ids = payload.identifiers || {}, catalog = Array.isArray(payload.platformCatalog) ? payload.platformCatalog : []; const inputs = [{ key: 'email', label: '邮箱', identifier: 'email', value: String(ids.email || '').trim() }, { key: 'phone', label: '手机号', identifier: 'phone', value: String(ids.phone || '').trim() }].filter(x => x.value); if (!inputs.length) return { findings: [], coverage: [], truth: [] }; const findings = [], coverageMap = new Map(), truth = []; for (const input of inputs) { const platforms = catalog.filter(p => normalizeIdentifierList(p).includes(input.identifier)).slice(0, 180); truth.push({ identifierType: input.key, label: input.label, provider: 'platform_catalog', apiExecuted: false, deterministicResult: false, reason: `平台目录覆盖只表示这些平台通常提供${input.label}找回/绑定入口；没有查询该${input.label}是否真实注册。` }); for (const p of platforms) { const key = p.id || p.name || p.domain || `${input.identifier}_${coverageMap.size}`; const item = coverageMap.get(key) || { platformId: p.id || '', platformName: p.name || '', domain: p.domain || '', supportedIdentifiers: new Set(), recoveryEntry: p.recoveryEntry || '', dataExportEntry: p.dataExportEntry || '', deletionEntry: p.deletionEntry || '', note: p.cleanupNote || '' }; item.supportedIdentifiers.add(input.label); coverageMap.set(key, item); } findings.push(createFinding({ scanId, subjectType: 'account', source: `${input.key}_identifier_coverage`, category: 'coverage_status', severity: 'info', confidence: 'verified', title: `${input.label}平台目录覆盖说明`, summary: `下方仅展示平台目录中的通用找回/导出/注销入口覆盖范围；不是账号命中、不是实际注册结果。`, evidenceType: 'self_declared_identifier', evidencePreview: `${input.label}已脱敏 · ${platforms.length} 个通用入口`, affectedIdentifierMasked: maskIdentifier(input.value, input.key), remediation: { actionType: 'verify_with_official_flow', label: `使用官方入口逐项确认${input.label}`, steps: ['不要把覆盖范围当成账号命中', '优先使用官方找回入口验证是否本人账号', '确认后再导出数据、解绑或注销'] } })); } const coverage = [...coverageMap.values()].map(item => ({ ...item, supportedIdentifiers: [...item.supportedIdentifiers], status: '目录覆盖，不是账号命中', certainty: 'not_deterministic', evidenceSource: 'platform_catalog_only', reason: '该平台目录声明支持邮箱/手机号等入口；未查询该号码或邮箱是否真实注册。' })).slice(0, 300); return { findings, coverage, truth }; }
async function probeUrl(url = '') { try { const res = await fetch(url, { method: 'GET', headers: { 'user-agent': 'OpenYourBox-SelfCheck/1.0' }, redirect: 'follow' }); const text = res.status === 200 ? (await res.text().catch(() => '')).slice(0, 800) : ''; return { status: res.status, ok: res.status === 200, blocked: [401,403,429,503].includes(res.status), noEvidence: res.status === 404 || res.status === 410 || /not found|page not found|用户不存在|页面不存在|该用户不存在|doesn't exist/i.test(text), preview: text.replace(/\s+/g, ' ').slice(0, 120) }; } catch (error) { return { status: 0, ok: false, blocked: false, noEvidence: false, error: safeMsg(error) }; } }
async function scanUsernamePublicProfiles(username = '', scanId = 'scan') { const u = String(username || '').trim().replace(/^@/, ''); if (!/^[\w.-]{2,64}$/.test(u)) return { findings: [], tasks: [], summary: { executed: 0, confirmed: 0, possible: 0, noEvidence: 0, failed: 0, skipped: true, reason: '用户名格式不适合公开 URL 探测。' } }; const selected = USERNAME_PUBLIC_PLATFORMS.slice(0, 36); const findings = [], tasks = []; let confirmed = 0, possible = 0, noEvidence = 0, failed = 0; for (const p of selected) { const url = p.url.replace('{u}', encodeURIComponent(u)); const r = await probeUrl(url); let label = '未发现证据', level = 'info', accountStatus = 'no_match', category = 'public_profile_no_evidence', severity = 'info'; if (r.ok && !r.noEvidence) { confirmed++; label = '已确认有账号/登录记录'; level = 'high'; accountStatus = 'public_profile_confirmed'; category = 'public_profile_confirmed'; severity = 'low'; } else if (r.blocked || (r.status >= 300 && r.status < 400)) { possible++; label = '可能存在账号'; level = 'medium'; accountStatus = 'public_profile_uncertain'; category = 'public_profile_uncertain'; severity = 'info'; } else if (r.noEvidence) { noEvidence++; } else { failed++; label = '查询失败'; level = 'medium'; accountStatus = 'query_failed'; category = 'public_profile_error'; }
    findings.push(createFinding({ scanId, subjectType: 'account', source: 'username_public_probe', category, severity, confidence: r.ok ? 'verified' : 'medium', title: `${p.name} 公开主页探测：${label}`, summary: `公开 URL 探测 ${url} 返回 HTTP ${r.status}${r.error ? `，${r.error}` : ''}。`, evidenceType: 'public_profile_url', evidencePreview: `${p.region} · ${url} · ${r.preview || ''}`, evidenceRef: url, affectedIdentifierMasked: maskIdentifier(u, 'username'), remediation: { actionType: 'review_public_profile', label: `核验 ${p.name}`, steps: ['打开公开主页人工确认是否本人', '若是本人账号，检查公开资料、导出数据与注销入口', '若非本人，标记为误报'] } }));
    if (['public_profile_confirmed','public_profile_uncertain'].includes(accountStatus)) tasks.push({ id: localId(`task_public_${p.id}`), platformId: p.id, platformName: p.name, accountStatus, taskType: 'public_profile_review', taskStatus: 'todo', actionUrl: url, recoveryEntry: url, dataExportEntry: '', deletionEntry: '', evidenceSource: 'username_public_probe', statusLabel: label, statusLevel: level, statusReason: label === '已确认有账号/登录记录' ? '公开主页返回 200 且未出现明显不存在提示；需要人工确认是否本人。' : '公开主页返回受限/重定向，无法确定是否本人账号。', notes: `公开资料探测：${url}`, createdAt: now(), updatedAt: now() }); }
  return { findings, tasks, summary: { executed: selected.length, confirmed, possible, noEvidence, failed, skipped: false, note: '同步公开探测限量执行；大陆数千平台需要后续队列/目录分批，不会在单次请求中撞库。' } };
}
function usernameNicknameMatrix(payload = {}, scanId = 'scan') { const ids = payload.identifiers || {}, username = String(ids.username || ids.nickname || '').trim(); if (!username) return { findings: [], tasks: [] }; const catalog = Array.isArray(payload.platformCatalog) ? payload.platformCatalog : []; const candidates = catalog.filter(p => { const identifiers = normalizeIdentifierList(p); const category = String(p.category || '').toLowerCase(); return identifiers.some(x => ['username', 'nickname', 'github_username', 'wechat_id', 'qq_number', 'gamertag'].includes(x)) || /social|community|creator|developer|forum|gaming|video|music|livestream|work|professional|microblog|content|knowledge|lifestyle|travel|local_life/i.test(category); }).slice(0, 150); const masked = maskIdentifier(username, 'username'); const finding = createFinding({ scanId, subjectType: 'account', source: 'username_nickname_matrix', category: 'platform_candidate_matrix', severity: 'info', confidence: 'medium', title: `用户名/昵称目录候选：${candidates.length} 个平台`, summary: '目录候选只提供后续核验入口；真实公开探测结果请看 username_public_probe。', evidenceType: 'self_declared_identifier', evidencePreview: `${masked} · ${candidates.slice(0, 18).map(p => p.name).join(', ')}`, affectedIdentifierMasked: masked, remediation: { actionType: 'account_cleanup', label: '逐个平台确认本人账号', steps: ['优先查看公开探测命中项', '再用官方找回入口确认是否为本人账号', '对不用的平台执行解绑、停用或注销'] } }); return { findings: [finding], tasks: [] }; }
function isVerifiedCompany(payload = {}) { return Boolean(payload.authorization?.verified || ['manual_admin_test', 'manual_admin', 'cloudflare_zone', 'dns_txt'].includes(payload.authorization?.method)); }
async function scanCertificateTransparencyReal(domain = '', scanId = 'scan') { const clean = value => String(value || '').toLowerCase().replace(/^https?:\/\//, '').replace(/^www\./, '').split('/')[0].replace(/[^a-z0-9.-]/g, '').replace(/^\.+|\.+$/g, ''); const d = clean(domain); if (!d) return []; try { const res = await fetch(`https://crt.sh/?q=${encodeURIComponent(`%.${d}`)}&output=json`, { headers: { 'user-agent': 'OpenYourBox-CTSummary/1.0' } }); if (!res.ok) throw new Error(`crt.sh HTTP ${res.status}`); const rows = await res.json(); const names = [...new Set((Array.isArray(rows) ? rows : []).flatMap(r => String(r.name_value || '').split(/\n+/)).map(clean).filter(x => x && (x === d || x.endsWith(`.${d}`))))].slice(0, 500); const suspicious = names.filter(x => /(dev|test|stage|staging|admin|vpn|internal|old|backup|legacy|uat)/i.test(x)); return [{ id: localId('finding_ct'), scanId, subjectType: 'domain', source: 'certificate_transparency', category: 'subdomain_inventory', severity: suspicious.length ? 'medium' : names.length > 50 ? 'low' : 'info', confidence: 'verified', title: `CT subdomain summary: ${d}`, summary: `Certificate Transparency returned ${names.length} unique domain names; suspicious naming samples ${suspicious.length}. Raw CT rows are not stored.`, evidenceType: 'certificate_transparency', evidencePreview: `sample=${names.slice(0, 12).join(', ') || 'none'}`, remediation: { actionType: 'review_asset', label: 'Review CT subdomains', steps: ['Confirm each subdomain is owned', 'Retire stale hosts', 'Review admin/vpn/internal-like names'] }, createdAt: now() }]; } catch (error) { return [{ id: localId('finding_ct_error'), scanId, subjectType: 'domain', source: 'certificate_transparency', category: 'adapter_error', severity: 'info', confidence: 'low', title: 'CT lookup failed', summary: safeMsg(error), evidenceType: 'certificate_transparency', evidencePreview: d, remediation: { actionType: 'review', label: 'Retry CT lookup', steps: ['Retry later or verify with another CT data source'] }, createdAt: now() }]; } }
function mergeExtraFindings(report, extra = [], subjectType = 'personal') { if (!extra.length) return report; const seen = new Set((report.findings || []).map(f => `${f.source}:${f.category}:${f.title}`)); for (const item of extra) { const key = `${item.source}:${item.category}:${item.title}`; if (!seen.has(key)) { report.findings.push(item); seen.add(key); } } report.riskScore = scoreRisk(report.findings, subjectType); report.dataSources = [...new Set([...(report.dataSources || []), ...extra.map(f => f.source)])]; report.remediationSteps = [...new Set(report.findings.flatMap(f => f.remediation?.steps || []))].slice(0, 20); return report; }
function mergeTasks(report, tasks = []) { if (!tasks.length) return report; const seen = new Set((report.accountTasks || []).map(t => `${t.platformId}:${t.taskType}:${t.platformName}:${t.evidenceSource || ''}`)); for (const task of tasks) { const key = `${task.platformId}:${task.taskType}:${task.platformName}:${task.evidenceSource || ''}`; if (!seen.has(key)) { report.accountTasks.push(task); seen.add(key); } } return report; }
export async function handleScan(context, mode) { const denied = gate(context.request, context.env); if (denied) return denied; try { const payload = ensureCatalog(await body(context.request)); payload.authorization ||= { mode: 'private', verified: false }; const runner = { personal: runPersonalScan, company: runCompanyScan, social: runSocialScan, accounts: runAccountsScan }[mode]; if (!runner) return jsonError('INVALID_SCAN_MODE', 400, 'Unsupported scan mode.'); const report = await runner(payload, context.env); if (mode === 'personal') report.findings = (report.findings || []).filter(f => f.source !== 'hibp'); if (mode === 'company') report.findings = (report.findings || []).filter(f => f.source !== 'shodan' && !(f.source === 'certificate_transparency' && /adapter ready/i.test(f.title || ''))); let extra = []; if (mode === 'personal') { const apiStatus = await identifierApiStatus(payload, context.env, report.scanId); extra.push(...apiStatus.findings); report.identifierApiStatus = apiStatus.status; if (payload.identifiers?.email) extra.push(...await runHibpEmailRange(payload.identifiers.email, context.env, Boolean(payload.authorization?.verified), report.scanId)); const coverage = identifierCoverageMatrix(payload, report.scanId); extra.push(...coverage.findings); report.identifierCoverageSuggestions = coverage.coverage; report.identifierTruthStatement = [...apiStatus.status, ...coverage.truth]; const matrix = usernameNicknameMatrix(payload, report.scanId); extra.push(...matrix.findings); const username = String(payload.identifiers?.username || payload.identifiers?.nickname || '').trim(); if (username) { const publicProbe = await scanUsernamePublicProfiles(username, report.scanId); extra.push(...publicProbe.findings); mergeTasks(report, publicProbe.tasks); report.usernamePublicProbeSummary = publicProbe.summary; if (!payload.identifiers?.github) extra.push(...await scanGithubPublic(username, context.env, report.scanId)); } } else if (mode === 'company') extra = [...(isVerifiedCompany(payload) && payload.domain ? await scanCertificateTransparencyReal(payload.domain, report.scanId) : []), ...await runExternalChecks(mode, payload, context.env, report.scanId)]; mergeExtraFindings(report, extra, mode === 'company' ? 'company' : 'personal'); enrichAccountStatuses(report); cache.set(report.scanId, report); const persistence = await saveReport(context.env, report); const status = persistence.persisted ? 'completed' : 'completed_not_persisted'; return response({ status, taskId: report.scanId, reportId: report.id, ...persistence, report }); } catch (error) { return jsonError('SCAN_FAILED', 500, 'Scan failed with a sanitized runtime error.', safeMsg(error)); } }
export async function getReport(context) { const report = await readReport(context.env, context.params?.id || ''); return report ? response(report) : jsonError('REPORT_NOT_FOUND', 404, 'Report not found.'); }
export async function listReports(context = {}) { const d = db(context.env || {}); if (d?.prepare) { try { const rows = await d.prepare('select id, job_id, report_type, risk_score, created_at from reports order by created_at desc limit 50').all(); return response({ reports: (rows.results || []).map(r => ({ id: r.id, scanId: r.job_id, reportType: r.report_type, riskScore: r.risk_score, createdAt: r.created_at })) }); } catch (error) { return jsonError('D1_READ_FAILED', 500, 'Could not read reports from D1.', safeMsg(error)); } } return response({ reports: [...cache.values()].map(r => ({ id: r.id, scanId: r.scanId, reportType: r.reportType, riskScore: r.riskScore?.total || 0, createdAt: r.generatedAt })) }); }
export async function exportReport(context) { const report = await readReport(context.env, context.params?.id || ''); if (!report) return jsonError('REPORT_NOT_FOUND', 404, 'Report not found.'); const format = new URL(context.request.url).searchParams.get('format'); if (format === 'md') return response(reportToMarkdown(report), 200, 'text/markdown; charset=utf-8'); if (format === 'csv') return response(reportToCsv(report), 200, 'text/csv; charset=utf-8'); return response(report); }
export async function getTask() { return jsonError('TASK_NOT_FOUND', 404, 'Task endpoint is reserved.'); }
export async function handleUpload(context, kind) { const denied = gate(context.request, context.env); if (denied) return denied; try { const text = await context.request.text(); const uploads = kind === 'mailbox' ? { mailboxText: text } : kind === 'browser-history' ? { browserText: text } : kind === 'platform-export' ? { platformExportText: text } : { passwordManagerText: text }; const report = await runPersonalScan(ensureCatalog({ uploads, authorization: { mode: 'private', verified: false } }), context.env); enrichAccountStatuses(report); cache.set(report.scanId, report); const persistence = await saveReport(context.env, report); return response({ status: 'processed', kind, reportId: report.id, taskId: report.scanId, ...persistence, report }); } catch (error) { return jsonError('UPLOAD_PROCESS_FAILED', 500, 'Upload text could not be processed.', safeMsg(error)); } }
export async function exportData(context = {}) { const d = db(context.env || {}); if (d?.prepare) { try { const rows = await d.prepare('select report_json from reports where user_id = ? order by created_at desc limit 100').bind('local').all(); return response({ reports: (rows.results || []).map(r => enrichAccountStatuses(JSON.parse(r.report_json))) }); } catch (error) { return jsonError('D1_EXPORT_FAILED', 500, 'Could not export report data.', safeMsg(error)); } } return response({ reports: [...cache.values()].map(enrichAccountStatuses) }); }
export async function deleteData(context = {}) { const denied = context.request ? gate(context.request, context.env) : null; if (denied) return denied; cache.clear(); const d = db(context.env || {}); if (d?.prepare) { try { await d.prepare('delete from findings where user_id = ?').bind('local').run(); await d.prepare('delete from account_tasks where user_id = ?').bind('local').run(); await d.prepare('delete from jobs where user_id = ?').bind('local').run(); await d.prepare('delete from reports where user_id = ?').bind('local').run(); return response({ status: 'cleared', persisted: true }); } catch (error) { return jsonError('D1_DELETE_FAILED', 500, 'Could not delete test report data.', safeMsg(error)); } } return response({ status: 'cleared', persisted: false }); }
async function txtRecords(domain = '') { const res = await fetch(`https://cloudflare-dns.com/dns-query?name=${encodeURIComponent(domain)}&type=TXT`, { headers: { accept: 'application/dns-json' } }); if (!res.ok) throw new Error(`DNS TXT ${res.status}`); const data = await res.json(); return (data.Answer || []).map(r => String(r.data || '').replace(/^"|"$/g, '').replace(/"\s+"/g, '')); }
function cleanDomain(value = '') { return String(value || '').toLowerCase().replace(/^https?:\/\//, '').replace(/^www\./, '').split('/')[0].replace(/[^a-z0-9.-]/g, '').replace(/^\.+|\.+$/g, ''); }
export async function verifyDnsTxt(context = {}) { const denied = gate(context.request, context.env); if (denied) return denied; try { const input = await body(context.request); const domain = cleanDomain(input.domain || ''); if (!domain) return jsonError('INVALID_DOMAIN', 400, 'A domain is required.'); const salt = context.env?.HASH_SALT || 'open-your-box-dns-txt'; const token = input.token || `oyb-${(await hashIdentifier(domain, salt)).slice(0, 24)}`; const recordName = `_openyourbox.${domain}`; if (!input.check) return response({ status: 'token_generated', domain, recordName, token, txtValue: token, instructions: `Add TXT ${recordName} = ${token}, then call this endpoint again with check:true.` }); const records = await txtRecords(recordName); const verified = records.some(value => value.includes(token)); return response({ status: verified ? 'verified' : 'pending', domain, recordName, token, verified, authorization: verified ? { mode: 'private', verified: true, method: 'dns_txt' } : { mode: 'private', verified: false, method: 'dns_txt_pending' } }); } catch (error) { return jsonError('DNS_TXT_VERIFY_FAILED', 500, 'DNS TXT verification failed.', safeMsg(error)); } }
