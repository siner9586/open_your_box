# Product flow

The application flow is user input, scope confirmation, adapter execution, masking, scoring, report output and follow-up task state.

Runtime code is in `src/scanners/core/runtime.mjs`. Cloudflare entry points are in `functions/api/`.
