const app = document.querySelector('#app');
const $ = (selector, root = document) => root.querySelector(selector);
const esc = (value = '') => String(value ?? '').replace(/[&<>'"]/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' }[char]));
const navPath = () => location.pathname.endsWith('/') ? location.pathname : `${location.pathname}/`;
const reportUrl = id => `/reports/?id=${encodeURIComponent(id || '')}`;
const STATUS_ORDER = ['已确认有账号/登录记录', '可能存在账号', '候选待确认', '未发现证据', '跳过', '查询失败', '待人工确认'];

async function apiPost(path, body = {}) { const res = await fetch(path, { method: 'POST', headers: { 'content-type': 'application/json', 'idempotency-key': crypto.randomUUID(), 'x-private-token': localStorage.getItem('oyb-token') || 'private-mode-token' }, body: JSON.stringify(body) }); const text = await res.text(); try { return JSON.parse(text); } catch { return { error: { code: 'NON_JSON_RESPONSE', message: text.slice(0, 300), status: res.status } }; } }
async function apiGet(path) { const res = await fetch(path, { headers: { accept: 'application/json', 'x-private-token': localStorage.getItem('oyb-token') || 'private-mode-token' } }); const text = await res.text(); try { return JSON.parse(text); } catch { return { error: { code: 'NON_JSON_RESPONSE', message: text.slice(0, 300), status: res.status } }; } }
async function apiAdminGet(path, token) { const res = await fetch(path, { headers: { accept: 'application/json', 'x-admin-token': token || '' } }); const text = await res.text(); try { return JSON.parse(text); } catch { return { error: { code: 'NON_JSON_RESPONSE', message: text.slice(0, 300), status: res.status } }; } }
async function apiAdminPost(path, body, token) { const res = await fetch(path, { method: 'POST', headers: { 'content-type': 'application/json', 'x-admin-token': token || '' }, body: JSON.stringify(body || {}) }); const text = await res.text(); try { return JSON.parse(text); } catch { return { error: { code: 'NON_JSON_RESPONSE', message: text.slice(0, 300), status: res.status } }; } }
async function catalog() { try { return (await (await fetch('/data/personal-account-platform-catalog.json')).json()).platforms || []; } catch { return []; } }
function card(title, text) { return `<article class="card"><h3>${esc(title)}</h3><p>${esc(text)}</p></article>`; }
function badge(value = 'info') { return `<span class="badge ${esc(value)}">${esc(value)}</span>`; }
function statusMeta(t = {}) { if (t.statusLabel) return { label: t.statusLabel, level: t.statusLevel || 'info', reason: t.statusReason || t.notes || '按报告证据判断。' }; const raw = `${t.accountStatus || ''} ${t.taskType || ''} ${t.notes || ''} ${t.evidenceSource || ''}`.toLowerCase(); if (/adapter_error|query_failed|lookup_failed|failed/.test(raw)) return { label: '查询失败', level: 'medium', reason: '查询源返回错误、网络失败或平台风控，不能据此判断账号不存在。' }; if (/adapter_status|skipped|missing_|not_configured|authorization|limited/.test(raw)) return { label: '跳过', level: 'info', reason: '缺少授权、实名、连接配置或用户数据，当前未执行该项查询。' }; if (/no_match|not_found|zero|未发现/.test(raw)) return { label: '未发现证据', level: 'info', reason: '已检查的当前数据源未发现该平台线索，不代表平台绝对没有账号。' }; if (/confirmed|password_manager/.test(raw)) return { label: '已确认有账号/登录记录', level: 'high', reason: '你提供的登录记录或账号导入中出现该平台。' }; if (/mailbox|browser|platform_export|possible/.test(raw)) return { label: '可能存在账号', level: 'medium', reason: '邮箱、浏览器记录或平台导出元信息中出现该平台线索，需要进入官方入口确认。' }; if (/candidate_from_username|username_review|username_nickname_candidate|candidate/.test(raw)) return { label: '候选待确认', level: 'low', reason: '仅由用户名/昵称和平台目录生成，不代表已经确认存在账号。' }; return { label: '待人工确认', level: 'info', reason: '暂无足够证据确认状态，请使用官方入口核验。' }; }
function actionLinks(t = {}) { const links = [['找回', t.recoveryEntry || t.actionUrl], ['导出', t.dataExportEntry], ['注销/停用', t.deletionEntry]].filter(([, u]) => u); return links.map(([label, url]) => `<a class="btn" href="${esc(url)}" target="_blank" rel="noreferrer">${esc(label)}</a>`).join(''); }
function statusCounts(tasks = []) { return tasks.reduce((acc, t) => { const label = statusMeta(t).label; acc[label] = (acc[label] || 0) + 1; return acc; }, {}); }
function statusBoard(report = {}) { const summary = { ...statusCounts(report.accountTasks || []), ...(report.accountStatusSummary || {}) }; return `<div class="grid cols-4">${STATUS_ORDER.map(k => card(k, `${summary[k] || 0} 个平台`)).join('')}</div><p><small>说明：账号状态只来自可解释证据。邮箱/手机号的通用入口目录不会被算作账号状态；两个不同手机号显示同一批入口目录是正常的，因为那不是号码命中结果。</small></p>`; }
function findingGrid(report = {}) { return `<div class="grid cols-3">${(report.findings || []).map(f => `<article class="card solid">${badge(f.severity)}<h3>${esc(f.title)}</h3><p>${esc(f.summary)}</p><p><small>${esc(f.source)} · ${esc(f.category)} · ${esc(f.evidencePreview || '')}</small></p></article>`).join('') || '<article class="card">暂无发现项。</article>'}</div>`; }
function coverageGrid(report = {}) { const truth = report.identifierTruthStatement || []; const rows = report.identifierCoverageSuggestions || []; if (!truth.length && !rows.length) return ''; return `<section class="card"><h3>邮箱/手机号确定性说明</h3>${truth.map(t => `<p><strong>${esc(t.label)}：</strong>${t.apiExecuted ? '已执行可确认查询' : '未执行可确认查询'}。${esc(t.reason || '')}</p>`).join('')}<p><small>下列内容只是“平台目录覆盖/官方入口清单”，不是账号命中、不是查询结果、不是确定注册证据。</small></p><div class="grid">${rows.slice(0, 80).map(r => `<article class="task"><span><strong>${esc(r.platformName || r.platformId || '平台')}</strong><br><small><b>性质：</b>${esc(r.status || '目录覆盖，不是账号命中')}</small><br><small><b>支持入口：</b>${esc((r.supportedIdentifiers || []).join('/'))}</small><br><small><b>依据：</b>${esc(r.reason || '未查询该标识是否真实注册。')}</small><br><span class="actions">${actionLinks(r)}</span></span>${badge('非命中')}</article>`).join('') || '<article class="card">没有目录覆盖项。</article>'}</div></section>`; }
function taskGrid(report = {}) { const tasks = report.accountTasks || []; if (!tasks.length) return '<article class="card">暂无账号任务。提交用户名/昵称或上传账号线索后会显示；仅输入邮箱/手机号不会生成账号命中任务。</article>'; return STATUS_ORDER.map(label => { const group = tasks.filter(t => statusMeta(t).label === label); if (!group.length) return ''; return `<section class="card"><div class="section-head"><div><h3>${esc(label)}</h3><p>${group.length} 个平台</p></div>${badge(label)}</div><div class="grid">${group.map(t => { const m = statusMeta(t); return `<article class="task"><input type="checkbox" aria-label="任务状态"><span><strong>${esc(t.platformName || t.platformId || '未知平台')}</strong><br><small><b>状态：</b>${esc(m.label)} ｜ <b>依据：</b>${esc(m.reason)}</small><br><small><b>任务：</b>${esc(t.notes || '使用官方入口确认、导出、解绑或注销。')}</small><br><span class="actions">${actionLinks(t)}</span></span>${badge(m.level)}</article>`; }).join('')}</div></section>`; }).join(''); }
function renderReportSummary(report = {}) { const id = report.scanId || report.id; return `<section class="section card"><div class="section-head"><div><h2>报告 ${esc(report.id || '')}</h2><p>${esc(report.reportType)} · ${esc(report.generatedAt || '')}</p><p>主体：${esc(JSON.stringify(report.maskedSubject || {}))}</p></div><div class="score-ring" style="--score:${report.riskScore?.total || 0}"><strong>${report.riskScore?.total || 0}</strong></div></div><h3>账号状态摘要</h3>${statusBoard(report)}${coverageGrid(report)}<div class="actions"><a class="btn primary" href="${esc(reportUrl(id))}">打开报告详情</a><a class="btn" href="${esc(report.exports?.markdown || '#')}">Markdown</a><a class="btn" href="${esc(report.exports?.csv || '#')}">CSV</a></div><h3>账号状态与处理入口</h3>${taskGrid(report)}<h3>发现项</h3>${findingGrid(report)}</section>`; }
function home() { app.innerHTML = `<section class="hero"><div><span class="eyebrow">Open Your Box</span><h1>真实数字足迹与暴露面自查。</h1><p class="lead">面向本人、自有账号、自有域名、自有代码资产和企业授权资产，把上传记录、公开页面、DNS、HTTPS、GitHub 与可选外部 API 归并成报告和修复任务。</p><div class="actions"><a class="btn primary" href="/personal/">开始个人自查</a><a class="btn" href="/organization/">开始组织自查</a><a class="btn" href="/reports/">查看报告</a></div></div><div class="glass-stack"><article class="card floating solid"><h3>生产闭环</h3><p>Pages Functions 写入 D1，报告中心可刷新后读取。</p></article><article class="card floating solid"><h3>账号状态</h3><p>只展示有证据支撑的状态。</p></article><article class="card floating solid"><h3>数据原则</h3><p>目录覆盖不冒充账号命中。</p></article></div></section><section class="section grid cols-4">${['个人自查','组织自查','账号任务台','报告中心'].map(x => card(x, '真实扫描链路返回结构化 JSON；缺配置或未验证时明确跳过。')).join('')}</section>`; }
function personal() { app.innerHTML = `<section class="section"><h2>个人数字足迹自查 Beta</h2><p class="lead">邮箱/手机号当前只生成“确定性说明 + 通用入口覆盖清单”，不会被显示为账号命中；真正的账号状态来自用户名/昵称、账号导入、邮箱导出、浏览器记录或后期实名授权。</p><form class="card form" id="personal-form"><div class="grid cols-2"><label>本人邮箱<input id="email" autocomplete="email" placeholder="me@example.com"></label><label>本人手机号<input id="phone" autocomplete="tel" placeholder="+86 138****0000"><small id="phone-gate">手机号深度扫描默认未开放，需要实名认证、手机号归属验证和明确授权。</small></label><label>用户名/昵称<input id="username" placeholder="octocat / 常用昵称 / 平台 ID"></label><label>公开主页 URL<textarea id="publicUrls" placeholder="每行一个 URL，只检查你提供的页面\nhttps://example.com"></textarea></label></div><div class="grid cols-2"><label>账号管理器导出文本<textarea id="passwordManagerText" placeholder="url,username\nhttps://github.com,me@example.com"></textarea><small>只用于提取平台线索。</small></label><label>邮箱导出文本<textarea id="mailboxText" placeholder="From: security@example.com\nSubject: account notice"></textarea><small>只提取平台线索，不保存邮件正文。</small></label><label>浏览器书签/历史文本<textarea id="browserText" placeholder="https://github.com/settings/security"></textarea><small>只提取账号、安全、隐私、导出、注销相关 URL。</small></label><label>平台导出元信息文本<textarea id="platformExportText" placeholder="粘贴平台导出的索引、URL 或元信息"></textarea><small>用于生成账号清理任务。</small></label></div><div class="actions"><a class="btn" href="/identity/">身份与手机号验证</a><a class="btn" href="/consent/">授权说明</a></div><label class="check-row"><input type="checkbox" id="ok"><span>我确认只检查本人数据；测试阶段按本人确认启用最大可用权限。</span></label><button class="btn primary" id="go" disabled>提交个人自查</button></form><div id="out" class="result-area"></div></section>`; $('#ok').onchange = () => $('#go').disabled = !$('#ok').checked; $('#phone').oninput = () => { $('#phone-gate').textContent = $('#phone').value.trim() ? '手机号 deep scan 不会从前端开启；生产默认由后端 ENABLE_PHONE_DEEP_SCAN=false 拦截。' : '手机号深度扫描默认未开放，需要实名认证、手机号归属验证和明确授权。'; }; $('#personal-form').onsubmit = async event => { event.preventDefault(); $('#out').innerHTML = '<div class="card">扫描中……</div>'; const username = $('#username').value.trim(); const payload = { authorization: { mode: 'private', verified: true, method: 'self_test_max_access' }, identifiers: { email: $('#email').value.trim(), phone: $('#phone').value.trim(), username, nickname: username }, platformCatalog: await catalog(), publicProfileUrls: $('#publicUrls').value.split(/\n+/).map(x => x.trim()).filter(Boolean), uploads: { passwordManagerText: $('#passwordManagerText').value, mailboxText: $('#mailboxText').value, browserText: $('#browserText').value, platformExportText: $('#platformExportText').value } }; const data = await apiPost('/api/scan/personal', payload); $('#out').innerHTML = data.report ? renderReportSummary(data.report) : `<pre class="card">${esc(JSON.stringify(data, null, 2))}</pre>`; }; }
function organization() { app.innerHTML = `<section class="section"><h2>组织授权资产自查 Beta</h2><p class="lead">未验证时只跑 DNS、HTTPS Header、GitHub public summary；完成管理员确认、Cloudflare Zone 或 DNS TXT 验证后才允许外部 adapter。</p><form class="card form" id="org-form"><div class="grid cols-3"><label>域名检查<input id="domain" placeholder="example.com"></label><label>GitHub 组织/用户名<input id="githubOrg" placeholder="octocat"></label><label>自有 IP 检查<input id="ip" placeholder="93.184.216.34"></label></div><label>资产验证方式<select id="method"><option value="none">未验证：轻量公开检查</option><option value="manual_admin_test">手动管理员确认：允许授权适配器</option><option value="cloudflare_zone">Cloudflare Zone 验证：Token 可查到 zone</option><option value="dns_txt">DNS TXT 已验证</option></select></label><label class="check-row"><input type="checkbox" id="org-ok"><span>我确认域名、组织或 IP 属于自有或明确授权资产。</span></label><button class="btn primary" id="org-go" disabled>提交企业自查</button></form><article class="card"><h3>DNS TXT 验证</h3><p>生成 token 后添加 TXT：<code>_openyourbox.&lt;domain&gt;</code>，再执行检查。</p><div class="actions"><button class="btn" id="dns-token">生成 TXT token</button><button class="btn" id="dns-check">检查 TXT</button></div><pre id="dns-out"></pre></article><div id="org-out" class="result-area"></div></section>`; $('#org-ok').onchange = () => $('#org-go').disabled = !$('#org-ok').checked; let dnsToken = ''; $('#dns-token').onclick = async () => { const data = await apiPost('/api/verify/dns-txt', { domain: $('#domain').value.trim() }); dnsToken = data.token || ''; $('#dns-out').textContent = JSON.stringify(data, null, 2); }; $('#dns-check').onclick = async () => { const data = await apiPost('/api/verify/dns-txt', { domain: $('#domain').value.trim(), token: dnsToken, check: true }); if (data.verified) $('#method').value = 'dns_txt'; $('#dns-out').textContent = JSON.stringify(data, null, 2); }; $('#org-form').onsubmit = async event => { event.preventDefault(); $('#org-out').innerHTML = '<div class="card">扫描中……</div>'; const method = $('#method').value; const payload = { domain: $('#domain').value.trim(), githubOrg: $('#githubOrg').value.trim(), ip: $('#ip').value.trim(), authorization: { mode: 'private', verified: method !== 'none', method } }; const data = await apiPost('/api/scan/company', payload); $('#org-out').innerHTML = data.report ? renderReportSummary(data.report) : `<pre class="card">${esc(JSON.stringify(data, null, 2))}</pre>`; }; }
async function social() { const list = await catalog(); app.innerHTML = `<section class="section"><div class="section-head"><div><h2>社交账号目录</h2><p>用于本人账号找回、导出、注销和安全设置入口，不做陌生人枚举。</p></div>${badge(`${list.length} platforms`)}</div><div class="tool-controls"><button class="chip active" data-region="all">全部</button><button class="chip" data-region="global">全球</button><button class="chip" data-region="china">中国大陆</button></div><div id="grid" class="grid cols-3"></div></section>`; const render = region => $('#grid').innerHTML = list.filter(p => region === 'all' || p.region === region).slice(0, 120).map(p => `<article class="card"><span class="badge">${esc(p.region)}</span><h3>${esc(p.name)}</h3><p>${esc(p.category)}</p><div class="actions"><a class="btn" href="${esc(p.recoveryEntry || '#')}">找回</a><a class="btn" href="${esc(p.dataExportEntry || '#')}">导出</a><a class="btn" href="${esc(p.deletionEntry || '#')}">处理账号</a></div></article>`).join(''); render('all'); document.querySelectorAll('[data-region]').forEach(b => b.onclick = () => render(b.dataset.region)); }
async function accounts() { const list = await catalog(); const reports = await apiGet('/api/reports'); const latestPersonal = (reports.reports || []).find(r => r.reportType === 'personal'); let report = null; if (latestPersonal) report = await apiGet(`/api/reports/${encodeURIComponent(latestPersonal.scanId || latestPersonal.id)}`); app.innerHTML = `<section class="section"><h2>账号任务台</h2><p class="lead">账号任务只展示证据链支撑的状态；邮箱/手机号目录覆盖不作为账号任务。</p><div class="actions"><a class="btn" href="/personal/">生成个人报告</a><a class="btn" href="/reports/">报告中心</a></div>${report?.accountTasks?.length ? statusBoard(report) + taskGrid(report) : `<div class="grid">${list.slice(0, 50).map(p => `<article class="task"><input type="checkbox"><span><strong>${esc(p.name)}</strong><br><small>状态：待人工确认。${esc(p.cleanupNote || '先导出数据，再处理账号状态。')}</small><br><span class="actions">${actionLinks(p)}</span></span>${badge('default')}</article>`).join('')}</div>`}</section>`; }
async function reports() { const id = new URL(location.href).searchParams.get('id'); if (id) return reportDetail(id); const data = await apiGet('/api/reports'); app.innerHTML = `<section class="section"><h2>报告中心</h2><p class="lead">从 D1 读取最近报告。详情页使用查询参数打开，避免 Cloudflare Pages 动态路径打不开。</p><div class="grid">${(data.reports || []).map(r => { const id = r.scanId || r.id; return `<article class="card"><h3>${esc(r.id || r.scanId)}</h3><p>${esc(r.reportType)} · ${esc(r.riskScore)} · ${esc(r.createdAt || '')}</p><div class="actions"><a class="btn primary" href="${esc(reportUrl(id))}">打开详情</a><a class="btn" href="/api/reports/${esc(id)}/export?format=md">Markdown</a><a class="btn" href="/api/reports/${esc(id)}/export?format=csv">CSV</a></div></article>`; }).join('') || '<article class="card">暂无服务器报告。提交一次扫描后会显示。</article>'}</div></section>`; }
async function reportDetail(id) { const report = await apiGet(`/api/reports/${encodeURIComponent(id)}`); if (report.error) { app.innerHTML = `<section class="section"><h2>报告详情打不开</h2><p>没有在 D1 或本地缓存中找到该报告。请回到报告中心重新打开，或重新提交一次个人自查。</p><div class="actions"><a class="btn" href="/reports/">返回报告中心</a><a class="btn" href="/personal/">重新个人自查</a></div><pre class="card">${esc(JSON.stringify(report, null, 2))}</pre></section>`; return; } app.innerHTML = `<section class="section"><div class="section-head"><div><h2>报告详情</h2><p>${esc(report.reportType)} · ${esc(report.generatedAt)}</p><p>${esc(report.authorizedScope || '')}</p></div><div class="score-ring" style="--score:${report.riskScore?.total || 0}"><strong>${report.riskScore?.total || 0}</strong></div></div><div class="grid cols-2">${card('主体', JSON.stringify(report.maskedSubject || {}))}${card('数据源', (report.dataSources || []).join(', '))}${card('导出', `Markdown: ${report.exports?.markdown || ''} | CSV: ${report.exports?.csv || ''}`)}${card('复查计划', report.nextReviewPlan || '')}</div><h3>账号状态总览</h3>${statusBoard(report)}${coverageGrid(report)}<h3>哪些平台有账号/可能有账号/候选/跳过/失败</h3>${taskGrid(report)}<h3>发现项</h3>${findingGrid(report)}<h3>修复建议</h3><div class="card"><ol>${(report.remediationSteps || []).map(s => `<li>${esc(s)}</li>`).join('') || '<li>根据发现项逐项复核。</li>'}</ol></div><div class="actions"><a class="btn" href="${esc(report.exports?.markdown || '#')}">导出 Markdown</a><a class="btn" href="${esc(report.exports?.csv || '#')}">导出 CSV</a><button class="btn" id="delete-data">清除测试数据</button><a class="btn" href="/reports/">返回报告中心</a></div><pre id="delete-out"></pre></section>`; $('#delete-data').onclick = async () => { $('#delete-out').textContent = JSON.stringify(await apiPost('/api/settings/delete-data', { scope: 'local-test' }), null, 2); }; }
function risk() { app.innerHTML = `<section class="section"><h2>风险评分</h2><div class="grid cols-3">${['identity','exposure','account','breach','infrastructure','remediation'].map(x => card(x, '按发现数量、严重程度、置信度、修复价值和资产类型归一化。')).join('')}</div></section>`; }
function settings() { app.innerHTML = `<section class="section"><h2>设置与数据</h2><p class="lead">用于测试阶段导出或清除本地匿名 D1 报告数据。</p><div class="grid cols-2"><article class="card"><h3>导出我的数据</h3><p>导出当前 D1 中 local 测试报告。</p><button class="btn" id="export-data">导出 JSON</button></article><article class="card"><h3>清除服务器数据</h3><p>删除当前测试范围内 reports、jobs、findings、account_tasks。</p><button class="btn" id="delete-data">清除测试数据</button></article></div><pre class="card" id="settings-out"></pre></section>`; $('#export-data').onclick = async () => { $('#settings-out').textContent = JSON.stringify(await apiPost('/api/settings/export-data', { scope: 'local-test' }), null, 2); }; $('#delete-data').onclick = async () => { $('#settings-out').textContent = JSON.stringify(await apiPost('/api/settings/delete-data', { scope: 'local-test' }), null, 2); }; }
function safety() { app.innerHTML = `<section class="section"><h2>安全边界</h2><p class="lead">本项目只做本人、自有资产和授权资产的防御型自查。真实扫描依赖用户上传、用户连接、公开 URL 或资产验证，不提供陌生人查询、绕过限制或攻击性能力。</p><div class="grid cols-3">${['目录覆盖不是命中','候选不等于注册','未验证企业资产不跑外部 adapter'].map(x => card(x, '这是当前生产化闭环的硬边界。')).join('')}</div></section>`; }
function auth() { app.innerHTML = '<section class="section"><h2>登录预留</h2><p class="lead">后续阶段接入登录；当前第 1–4 步使用 local 匿名测试用户完成 D1 报告闭环。</p></section>'; }
function login() { app.innerHTML = `<section class="section"><h2>登录</h2><p class="lead">生产环境不开放 dev login；真实登录将在后续接入受控身份提供方。</p><form class="card form" id="login-form"><label>Demo 邮箱<input id="login-email" value="demo@example.com" autocomplete="email"></label><label>显示名<input id="login-name" value="Demo User"></label><button class="btn primary">Dev login</button><small>如果生产环境返回 403，说明 dev login 已按安全要求关闭。</small></form><pre class="card" id="login-out"></pre></section>`; $('#login-form').onsubmit = async event => { event.preventDefault(); const data = await apiPost('/api/auth/dev-login', { email: $('#login-email').value, display_name: $('#login-name').value }); if (data.session_token) { localStorage.setItem('oyb-session-token', data.session_token); localStorage.setItem('oyb-user-id', data.user_id); } $('#login-out').textContent = JSON.stringify(data, null, 2); }; }
function identity() { const userId = localStorage.getItem('oyb-user-id') || 'usr_demo'; app.innerHTML = `<section class="section"><h2>身份与授权</h2><p class="lead">手机号深度扫描需要实名 verified、手机号归属 verified、phone_deep_scan 授权、限流通过和审计记录。生产第一版默认关闭手机号深度扫描。</p><div class="grid cols-2"><form class="card form" id="real-form"><h3>实名认证</h3><label>User ID<input id="real-user" value="${esc(userId)}"></label><label>姓名<input id="real-name" placeholder="Demo User"></label><label>证件引用<input id="real-ref" placeholder="manual-review-ref"></label><button class="btn primary">开始实名认证</button><small>manual provider 下会进入人工审核，不会自动 verified。</small></form><form class="card form" id="phone-form"><h3>手机号归属验证</h3><label>User ID<input id="phone-user" value="${esc(userId)}"></label><label>手机号<input id="verify-phone-value" placeholder="+15550101000"></label><button class="btn primary">开始手机号验证</button><small>手机号只 hash 落库，页面只展示 mask。</small></form></div><div class="actions"><button class="btn" id="identity-status">刷新状态</button><a class="btn" href="/consent/">管理授权</a><a class="btn" href="/privacy-center/">隐私中心</a></div><pre class="card" id="identity-out"></pre></section>`; $('#identity-status').onclick = async () => { $('#identity-out').textContent = JSON.stringify(await apiGet(`/api/identity/status?user_id=${encodeURIComponent($('#real-user').value || userId)}`), null, 2); }; $('#real-form').onsubmit = async event => { event.preventDefault(); const data = await apiPost('/api/identity/start-real-name', { user_id: $('#real-user').value, name: $('#real-name').value, evidence_ref: $('#real-ref').value || 'manual-review' }); $('#identity-out').textContent = JSON.stringify(data, null, 2); }; $('#phone-form').onsubmit = async event => { event.preventDefault(); const data = await apiPost('/api/identity/start-phone-verification', { user_id: $('#phone-user').value, phone: $('#verify-phone-value').value }); $('#identity-out').textContent = JSON.stringify(data, null, 2); }; }
function consent() { const userId = localStorage.getItem('oyb-user-id') || 'usr_demo'; app.innerHTML = `<section class="section"><h2>授权管理</h2><p class="lead">提交前请确认查询对象、目的、范围、保存方式和撤销路径。手机号 deep scan 还必须通过实名和手机号归属验证。</p><form class="card form" id="consent-form"><div class="grid cols-2"><label>User ID<input id="consent-user" value="${esc(userId)}"></label><label>授权类型<select id="consent-type"><option value="personal_self_check">个人自查</option><option value="phone_deep_scan">手机号深度扫描</option><option value="report_export">报告导出</option><option value="data_retention">数据保留</option></select></label><label>目标类型<input id="consent-target-type" value="phone"></label><label>目标值<input id="consent-target-value" placeholder="+15550101000"></label></div><button class="btn primary">授予授权</button><button class="btn" type="button" id="revoke-consent">撤销授权</button></form><pre class="card" id="consent-out"></pre></section>`; $('#consent-form').onsubmit = async event => { event.preventDefault(); const data = await apiPost('/api/consent/grant', { user_id: $('#consent-user').value, consent_type: $('#consent-type').value, target_type: $('#consent-target-type').value, target_value: $('#consent-target-value').value, consent_version: '2026-07-01' }); $('#consent-out').textContent = JSON.stringify(data, null, 2); }; $('#revoke-consent').onclick = async () => { const data = await apiPost('/api/consent/revoke', { user_id: $('#consent-user').value, consent_type: $('#consent-type').value, target_type: $('#consent-target-type').value, target_value: $('#consent-target-value').value }); $('#consent-out').textContent = JSON.stringify(data, null, 2); }; }
function privacyCenter() { const userId = localStorage.getItem('oyb-user-id') || 'usr_demo'; app.innerHTML = `<section class="section"><h2>隐私中心</h2><p class="lead">支持创建导出、删除、匿名化请求；敏感标识 hash 存储，界面 mask 展示。</p><form class="card form" id="privacy-form"><label>User ID<input id="privacy-user" value="${esc(userId)}"></label><label>请求类型<select id="privacy-type"><option value="export">导出我的数据</option><option value="delete">删除我的数据</option><option value="anonymize">匿名化请求</option></select></label><label>范围<input id="privacy-scope" value="self"></label><button class="btn primary">创建请求</button></form><div class="actions"><a class="btn" href="/consent/">撤销授权</a><a class="btn" href="/privacy/">隐私说明</a></div><pre class="card" id="privacy-out"></pre></section>`; $('#privacy-form').onsubmit = async event => { event.preventDefault(); const data = await apiPost('/api/privacy/request', { user_id: $('#privacy-user').value, request_type: $('#privacy-type').value, scope: $('#privacy-scope').value }); $('#privacy-out').textContent = JSON.stringify(data, null, 2); }; }
function adminReviewsPage() { app.innerHTML = `<section class="section"><h2>管理员审核</h2><p class="lead">需要 ADMIN_TOKEN；列表只展示 masked target 和 evidence/ref，不展示身份证号、手机号或真实姓名明文。</p><form class="card form" id="admin-form"><label>ADMIN_TOKEN<input id="admin-token" type="password" autocomplete="off"></label><button class="btn primary">加载待审核</button></form><div id="admin-list" class="grid"></div><pre class="card" id="admin-out"></pre></section>`; $('#admin-form').onsubmit = async event => { event.preventDefault(); const token = $('#admin-token').value; const data = await apiAdminGet('/api/admin/reviews', token); $('#admin-out').textContent = JSON.stringify(data, null, 2); $('#admin-list').innerHTML = (data.reviews || []).map(r => `<article class="card"><h3>${esc(r.review_type)}</h3><p>${esc(r.target_masked || '')}</p><p><small>${esc(r.reason || '')}</small></p><div class="actions"><button class="btn primary" data-review="${esc(r.id)}" data-decision="approved">Approve</button><button class="btn" data-review="${esc(r.id)}" data-decision="rejected">Reject</button></div></article>`).join('') || '<article class="card">暂无待审核项，或 token 无效。</article>'; document.querySelectorAll('[data-review]').forEach(btn => btn.onclick = async () => { const result = await apiAdminPost('/api/admin/reviews/decide', { review_id: btn.dataset.review, decision: btn.dataset.decision }, token); $('#admin-out').textContent = JSON.stringify(result, null, 2); }); }; }
async function jobPage() { const id = new URL(location.href).searchParams.get('id') || ''; app.innerHTML = `<section class="section"><h2>异步任务</h2><form class="card form" id="job-form"><label>Job ID<input id="job-id" value="${esc(id)}"></label><button class="btn primary">查询</button></form><pre class="card" id="job-out"></pre></section>`; $('#job-form').onsubmit = async event => { event.preventDefault(); const jobId = $('#job-id').value.trim(); const status = await apiGet(`/api/scan/status?id=${encodeURIComponent(jobId)}`); const results = await apiGet(`/api/scan/results?id=${encodeURIComponent(jobId)}`); $('#job-out').textContent = JSON.stringify({ status, results }, null, 2); }; if (id) $('#job-form').dispatchEvent(new Event('submit')); }
async function coverage() { const data = await apiGet('/api/scan/adapters'); app.innerHTML = `<section class="section"><h2>覆盖范围</h2><p class="lead">展示 demo、skeleton 和 disabled adapter。手机号 deep scan 默认 disabled，不做跨平台撞库或登录态探测。</p><div class="grid cols-3">${(data.adapters || []).map(a => `<article class="card"><span class="badge">${a.enabled ? 'enabled' : 'disabled'}</span><h3>${esc(a.id)}</h3><p>${esc(a.description || '')}</p><small>${esc(a.target_type || '')} · ${esc(a.mode || '')}</small></article>`).join('')}</div></section>`; }
const routes = { '/': home, '/personal/': personal, '/organization/': organization, '/social/': social, '/accounts/': accounts, '/reports/': reports, '/risk/': risk, '/settings/': settings, '/auth/': auth, '/login/': login, '/identity/': identity, '/consent/': consent, '/privacy-center/': privacyCenter, '/admin/reviews/': adminReviewsPage, '/job/': jobPage, '/coverage/': coverage, '/safety/': safety };
function boot() { const path = navPath(); const handler = path.startsWith('/reports/') ? reports : (routes[path] || home); handler(); $('.nav-toggle')?.addEventListener('click', () => $('.site-nav')?.classList.toggle('open')); }
boot();
